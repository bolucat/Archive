[package]
name = "wind-core"
version.workspace = true
repository.workspace = true
edition.workspace = true
description.workspace = true
license = "MIT OR Apache-2.0"

[features]
default = ["quic"]
quic = ["quinn"]

[dependencies]
pin-project = "1"
tokio = { version = "1", default-features = false, features = ["io-util", "macros", "time", "net", "signal"] }
tokio-util = { version = "0.7", features = ["rt"] }

dashmap = "6"
uuid = "1"

quinn = { workspace = true, default-features = false, optional = true }

socket2 = "0.6"

serde = { version = "1", features = ["derive"] }

futures = "0.3"
bytes = "1"
eyre = "0.6"
ipnet = "2"
regex = "1"
pin-project-lite = "0.2"

tracing = "0.1"
rand = "0.10"
async-trait = "0.1"
bytesize = "2"

[dev-dependencies]
quinn-udp = "0.6"
serde_json = "1"